function remove(element) {
    element.remove();
}

function logout(element) {
    element.innerText = "Logout";
}

function liked(element) {
    element.innerText = "100 Likes"
}

function liked2(element) {
    element.innerText = "10000 Likes"
}